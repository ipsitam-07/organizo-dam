{
  "name": "gateway",
  "version": "1.0.0",
  "description": "Organizo DAM — API documentation gateway. Serves the merged Swagger UI for all services at a single URL.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/docs": "workspace:*",
    "@repo/logger": "workspace:*",
    "express": "^4.19.2",
    "helmet": "^8.0.0",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.24",
    "@types/swagger-ui-express": "^4.1.8",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
